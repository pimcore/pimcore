

if (typeof pimcore == "undefined") {
    pimcore = {};
}

pimcore.pdf = function (config) {
    this.id = config["id"];
    this.data = config["data"];

    this.init();
};

pimcore.pdf.prototype.init = function () {

    this.el = document.getElementById(this.id);
    this.
};



pimcore.pdf.prototype.hurarei = function () {

};
